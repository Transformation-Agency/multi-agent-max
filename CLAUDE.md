# Standing instructions — <<PROJECT NAME>>

Read `state/WHITEBOARD.md` first. Your role prompt is in `prompts/<role>.md`.
The spec is `SPEC.md`; the architecture and stack commands are `ARCHITECTURE.md`.

## Invariants (never relax, any stage)

1. **`scripts/verify.sh` exiting 0 is the only certificate of done.** Your summary is not evidence. A green build is a precondition, not a proof.
2. **Contracts change only via an ADR** in `state/DECISIONS.md`. If you need a contract change, stop and propose one.
3. **Never weaken a test to pass.** No `.skip`/`.only`, no deleted assertions, no loosened configs. Flaky → quarantine tag + ticket, never retry into green.
4. **Declare debt.** Any shortcut → `@debt` comment in code + entry in `state/DEBT.md` with your ticket id.
5. **Audit files are append-only** (`state/LESSONS.md`, `DECISIONS.md`, `DEBT.md`, `LEDGER.md`, `evidence/`). Never edit or delete prior lines.
6. **Only a Verifier sets `done`.**
7. **Stay in scope.** Build what the ticket says; list what you didn't do in `notes.md`.
8. **One unit at a time:** change → verify → commit. Rebase on main before `verify.sh`.
9. **Budget:** if you exceed your ticket's budget, stop, write a diagnosis, set `blocked`.
10. **Read before building.** Read the actual contract/construction you depend on, not a summary of it.

<!-- LESSONS:BEGIN — generated by scripts/gen-claude-md.sh from state/LESSONS.md; do not hand-edit -->
## Active lessons

### P1 · P · active · <<date>> · T-000
**Failure mode:** Treating a green build as proof a change is complete.
**Fix:** Run `scripts/verify.sh`; read the diff; map tests to AC ids.
**Rule:** Green build is a precondition, not a certificate. Only verify.sh exit 0 + verifier diff read counts.
<!-- LESSONS:END -->

Full lesson record (including retired): `state/LESSONS.md`.
